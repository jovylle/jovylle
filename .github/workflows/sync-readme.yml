name: "Profile README: Daily Projects Sync"

on:
  schedule:
    # Run every day at 6 AM UTC (adjust timezone as needed)
    - cron: '0 6 * * *'
  
  # Allow manual trigger from GitHub UI
  workflow_dispatch:
  
  # Run when sync script is updated
  push:
    paths:
      - 'sync-readme.js'

jobs:
  sync-readme-daily:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        
    - name: Generate README from Projects API
      run: node sync-readme.js
      
    - name: Commit README changes (if any)
      uses: stefanzweifel/git-auto-commit-action@v5
      with:
        commit_message: '🤖 Daily auto-update: README projects/tech stack'
        file_pattern: 'README.md'
        commit_user_name: 'github-actions[bot]'
        commit_user_email: 'github-actions[bot]@users.noreply.github.com'
